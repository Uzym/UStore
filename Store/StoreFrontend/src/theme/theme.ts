import { createTheme } from '@mui/material/styles'

export const theme = createTheme({
	typography: {
		fontFamily: ['__Roboto_4da422', '__Roboto_Fallback_4da422'].join(','),
		allVariants: {
			color: '#1F1F1F',
		},
	},
})
