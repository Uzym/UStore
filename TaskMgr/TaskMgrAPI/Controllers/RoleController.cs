using Microsoft.AspNetCore.Mvc;
using TaskMgrAPI.Context;

namespace TaskMgrAPI.Controllers
{
    public class RoleController : Controller
    {
        // private readonly TaskmgrContext _context;
        // public RoleController(TaskmgrContext context)
        // {
        //     _context = context;
        // }
        //[Route("user/{user_id}/card/{card_id}")]
        //public async UserCard(long user_id, long card_id)
        //{

        //}
        //[Route("user/{user_id}/project/{project_id}")]

    }
}
